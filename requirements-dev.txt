# Build / reproduction-only dependencies (NOT needed by the deployed app).
# Install alongside requirements.txt to run scripts/run_part_b.py end to end:
#     pip install -r requirements.txt -r requirements-dev.txt
# Keep these OUT of the deployed app so Streamlit cold starts stay fast - the app
# should load precomputed artifacts from results/, not score sentiment at runtime.
nltk>=3.8
# finVADER provides the finance lexicons (SentiBignomics + Henry) merged into
# VADER for the finvader_compound scores. Build/reproduction only - the deployed
# app reads precomputed results/, it never scores headlines.
finvader>=1.0.2
# vaderSentiment supplies the VADER lexicon used by features.py's Part A
# word-count exhibits (positive/negative/neutral counts, lexicon-based word
# counts). Imported lazily inside those functions only - not on the Part B
# backtest/sentiment path and NOT needed by the deployed app. Without it the
# functions fall back to a tiny placeholder lexicon and silently undercount,
# so declare it here for faithful reproduction on another machine.
vaderSentiment>=3.3.2
